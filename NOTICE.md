# NOTICE

Copyright 2026 Fleet contributors (Apache-2.0)

This product distributes and includes:

1. **Official goose binaries** (Apache-2.0), Copyright AAIF and goose contributors,
   from https://github.com/aaif-goose/goose official releases. Version matrix:
   `runtime/versions.json`. Binaries are fetched unmodified and SHA256-verified.
2. **UI source snapshot** derived from `ui/desktop` of the goose repository at
   baseline commit `f4066f1` (fork of upstream `0e17bf7`). Snapshot files retain
   their upstream copyright headers; modifications made after the snapshot are
   marked with `Modified by Fleet` headers.

"goose" is a trademark of its respective owners; Fleet uses the name in
nominative use only ("orchestrates goose agents") and is not affiliated with the
goose project.
