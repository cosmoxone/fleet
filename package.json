{
  "name": "@fleet/shell",
  "private": true,
  "version": "0.1.0",
  "description": "Fleet — multi-agent ACP orchestrator shell (goose is the first driver)",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "node-cli": "tsx core/cli.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.9.3",
    "vitest": "^4.0.18"
  }
}
